let path = require('path');
let expect = require('chai').expect;
let {checkDiagonalWinner} = require('../checkDiagonalWinner');

describe('checkDiagonalWinner()', () => {
  'use strict';
  it('', () => {

  });
});
