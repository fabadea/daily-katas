let path = require('path');
let expect = require('chai').expect;
let {runLengthEncoder} = require('../runLengthEncoder');

describe('runLengthEncoder()', () => {
  'use strict';
  it('', () => {

  });
});
